%!tikz editor 1.0
\documentclass{article}
\usepackage{tikz}
\usepackage[graphics, active, tightpage]{preview}
\PreviewEnvironment{tikzpicture}

%!tikz preamble begin

%!tikz preamble end


\begin{document}
%!tikz source begin
\begin{tikzpicture}

%lattice
\foreach \x in {1,...,7}
  \foreach \y in {1,...,7}
  \draw (\x,\y) circle (0.3cm);

  % error
  \foreach \x/\y in {4/1,2/3,1/5,2/6,6/7,7/4}
  \fill[yellow] (\x,\y) circle (0.3cm);

%  \draw[blue][->]        (1,1.5)   -- (7,1.5);

\foreach \y in {1,...,7}
\draw[blue][dashed]        (1,\y)   -- (7,\y);


\end{tikzpicture}
%!tikz source end

\end{document}