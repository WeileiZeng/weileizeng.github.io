%!tikz editor 1.0
\documentclass{article}
\usepackage{tikz}
\usepackage[graphics, active, tightpage]{preview}
\PreviewEnvironment{tikzpicture}

%!tikz preamble begin

%!tikz preamble end


\begin{document}
%!tikz source begin
\begin{tikzpicture}

%lattice
\foreach \x in {1,...,7}
  \foreach \y in {1,...,7}
  \draw (\x,\y) circle (0.3cm);

  % error
  \foreach \x/\y in {3/1,3/2}
  \fill[yellow] (\x,\y) circle (0.3cm);


\foreach \y in {1,...,7}
\draw[blue][dashed]        (1,\y)   -- (7,\y);

%\draw[blue][->]        (1,1.5)   -- (7,1.5);

\end{tikzpicture}
%!tikz source end

\end{document}